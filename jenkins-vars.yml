---

# jenkins variables
project_name: docker-darktable
external_type: na
custom_version_command: |-
  curl -sL https://archlinux.org/packages/extra/x86_64/darktable/ |awk -F'(<h2>darktable |</h2>)' '/<h2>/ {print $2}'
release_type: stable
release_tag: latest
ls_branch: master
build_armhf: false
repo_vars:
  - BUILD_VERSION_ARG = 'DARKTABLE_VERSION'
  - LS_USER = 'linuxserver'
  - LS_REPO = 'docker-darktable'
  - CONTAINER_NAME = 'darktable'
  - DOCKERHUB_IMAGE = 'linuxserver/darktable'
  - DEV_DOCKERHUB_IMAGE = 'lsiodev/darktable'
  - PR_DOCKERHUB_IMAGE = 'lspipepr/darktable'
  - DIST_IMAGE = 'arch'
  - MULTIARCH = 'true'
  - CI = 'true'
  - CI_WEB = 'true'
  - CI_PORT = '3001'
  - CI_SSL = 'true'
  - CI_DELAY = '120'
  - CI_DOCKERENV = 'TZ=US/Pacific'
  - CI_AUTH = 'user:password'
  - CI_WEBPATH = ''
